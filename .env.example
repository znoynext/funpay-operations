# Copy to .env and adjust only non-sensitive local values.
FUNPAY_MANAGER_CONFIG=config.yaml
FUNPAY_MANAGER_LOG_LEVEL=INFO

# Secrets are intentionally not placed here. Store them with `funpay-setup`.
